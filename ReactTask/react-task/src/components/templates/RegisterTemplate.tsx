import { Form } from '../organisms/form/Form'
import { RegisterContainer } from '../styles/formStyle/formTemplate.style';

const RegisterTemplate = () => {
  return (
    <RegisterContainer>
      <Form formHeading="Register" />
    </RegisterContainer>
  );
};

export default RegisterTemplate;